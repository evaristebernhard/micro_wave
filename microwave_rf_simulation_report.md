---
title: "微波加热设备天线点板及连接件"
subtitle: "射频仿真设计报告：数学可行性、二维计算与级联验证"
author: "工程技术评审稿 V1.0"
date: "2026年9月14日"
lang: zh-CN
---

# 阅读说明与结论摘要

**本报告不是已经完成 HFSS/CST 三维全波求解的产品验收报告。** 本次实际完成了功率守恒与可行性证明、二维介质覆盖微带截面的有限体积计算、匹配级联链的解析计算、包含多次反射的两端口网络求解程序及单元测试。三维天线的 S 参数、3 cm 处场分布、实际被加热物吸收功率、触点热点和温升，尚无可核验的全波/热求解结果，不作“已达标”陈述。

本报告直接依据用户提供的完整需求书，不假设存在未提供的机械图、负载参数或测试记录。为形成可复算的频率相关算例，采用 **2.45 GHz 作为示例工况**，另以 915 MHz 对电缆作对照；这两个频率均不是对项目实际工作频率的确认。

## 本次评审的核心结论

| 事项 | 数学或计算结论 | 工程含义 |
|:--|:--|:--|
| 500 W、100块、每块至少5 W | 100块已需500 W有效功率，任何正损耗都会破坏约束 | 原始指标不能全部同时兑现 |
| 再要求每板实际取能时直通插损不超过0.5 dB | 在匹配、单向、独立级联模型下，100块至少需540.98 W板链入口功率 | 该下界尚未包含电缆和材料损耗 |
| 相同固定耦合板靠调主机功率补偿 | 线性系统中改变源幅度不会改变各板功率比 | 必须改变分配网络或耦合自由度 |
| RG142，5 m不超过0.5 dB | 所核厂家型号在2.45 GHz算例中仅线缆本体约3.886 dB | 不能用连接薄片优化抵消有损电缆的本体损耗 |
| FR4上2 mm走线 | PP覆盖的二维简化计算约59 Ω；并非天然50 Ω | 应以实际层叠重新设计阻抗 |
| 10 cm FR4桥、0.2 dB指标 | 假设FR4损耗角正切0.02，二维参与率估算仅介质损耗约0.676 dB | 指标与该假设材料冲突，需低损耗材料或改传输结构 |
| 每端1个能量触点、2个识别触点 | 清单未定义跨模块的射频地回流接口 | 不能在模型里凭空补一条理想接地路径 |

**可推进的方向不是直接在原有四个几何变量内“优化到全部达标”，而是先修正功率预算、区分有用取能与寄生损耗、定义射频回流，再用分级耦合或多支路供能建立可行设计域。**

## 证据等级

本文标注以下四类证据：**严格推导**指在明确假设内可由方程证明的结论；**本次计算**指随附件代码实际执行的数据；**工程假设**指用于算例但尚未由用户或供应商确认的输入；**待验证**指必须依赖真实三维几何、材料、负载或实测的结果。四者不可混用。特别是，网络模型内功率完全均匀，不能被写成真实天线阵列已经均匀。

\newpage

# 1. 输入条件、待冻结参数与计算边界

## 1.1 原始约束的保留

灰板外形50 mm×50 mm；前板为2 mm PP，FR4基板1.6 mm，背板为6 mm PP；相对介电常数分别按用户给定2.2、4.4、2.2保留。灰板正面包含直通能量线、耦合段、2至4圈方形螺旋线圈及连接10 kΩ电阻的识别线；背面整片铜地。耦合段长度5至15 mm、间距0.3至0.8 mm、直通线宽1.5至3 mm为原始优化范围。

平面桥长100 mm、宽50 mm、高30 mm；立体桥两边各50 mm、宽50 mm、高30 mm。桥和连接薄片使用35 μm铜，能量线宽2 mm，识别线宽0.3 mm，两类走线间距至少1 mm。各磁吸端列明两颗直径5 mm、厚2 mm的N35H磁铁，一个3 mm×5 mm能量弹片，两个1 mm×3 mm识别弹片。连接线含50 Ω RG142同轴、两芯屏蔽信号线及N型接口，长度3至10 m。

这些是输入规格，不是已经通过射频验证的合理性结论。PP颜色、透明度和磁吸外形不能替代介电损耗、接触电阻或回流阻抗的材料输入。

## 1.2 三维仿真前必须冻结的输入

| 输入项目 | 当前状态 | 不明确时造成的后果 |
|:--|:--|:--|
| 中心频率、工作带宽、谐波及频率漂移 | 未给出 | 无法确定电长度、谐振、介质/同轴损耗 |
| 500 W参考面 | 未给出 | 主机口、线缆末端、板链入口的预算会不同 |
| 连续波、脉冲峰值、占空比和加热时长 | 未给出 | 场强、平均温升和热容要求不同 |
| “单板取能”是天线端口接受功率还是工件吸收功率 | 未定义 | 同一个5 W可能代表完全不同的加热效果 |
| 工件几何、位置、复介电常数、温度变化 | 未给出 | 谐振、失配、功率归属和温升无法求出 |
| 3 cm测量基准和完整有效区域 | 未定义 | 近场均匀性无法复验 |
| 5/25/100块实际空间排布和板间间隙 | 未给出 | 不能确定空间互耦、边缘效应和桥的位置 |
| 铜形状、线圈外径、线距、开口及接法 | 未给出 | 匝数不能唯一确定天线 |
| PP与铜之间的胶层、气隙和装配压缩量 | 未给出 | 阻抗与局部电场都可能改变 |
| FR4、PP牌号及复介电常数随频温变化 | 仅有实部名义值 | 无法可信计算插损和自热 |
| 接地接触形状、端口接法、接触压力和镀层 | 不完整 | 理想模型可能掩盖真实回流中断 |
| 末端负载的额定功率、反射系数、冷却 | 未给出 | 末端功率和异常工况不明确 |
| 主机源阻抗、保护行为、功率下限 | 未给出 | 不能仅凭板数构造安全控制律 |

桥的“高30 mm”和“厚3至5 mm”应明确分别指整体高度、壳壁厚度还是板厚。连接薄片的N母座安装位置、同轴芯线与屏蔽层终端方式也需要剖面图。不得把机械语义上的歧义转成一组未经确认的CAD尺寸。

组间跨接也必须澄清：若A组末端已经接匹配吸收负载，不能同时把该端视为无损输出给B组。若改成A接B的长链，B入口功率不会自动恢复到500 W，而满足A末端输出乘连接线传输效率的约束。

## 1.3 本次计算采用的假设

二维截面取零厚度理想导体、连续背面地、1.6 mm FR4及直接贴合的2 mm PP覆盖层；无胶层、无气隙、无侧邻走线。基板实部介电常数4.4，PP为2.2。损耗示例取FR4的 $\tan\delta=0.02$，并暂把PP损耗取零，以分离FR4贡献。这不是对实际PP零损耗的断言。

级联例子默认各级互不通过自由空间耦合、源和终端匹配、取出的功率全部被计为有效功率。后一个条件是乐观上界；真实工件吸收效率不足1时，所需输入功率更高。具体频率、材料和负载变化后必须重算。

# 2. 指标定义：先消除“插损”和“取能”的歧义

## 2.1 端口功率与S参数

所有报告端口统一为实数50 Ω参考阻抗。对峰值相量电压、电流，定义功率波

$$
a=\frac{V+Z_0I}{2\sqrt{2Z_0}},\qquad
b=\frac{V-Z_0I}{2\sqrt{2Z_0}}.
$$

于是 $P_{\rm net}=|a|^2-|b|^2=\tfrac12\operatorname{Re}(VI^*)$。若求解器使用RMS相量，应去掉上述定义分母中的 $\sqrt2$，不能混用归一化。

在其余端口均匹配时，

$$
IL=-20\log_{10}|S_{21}|,\qquad
\Gamma=S_{11},\qquad
VSWR=\frac{1+|\Gamma|}{1-|\Gamma|}.
$$

因而VSWR不超过2.0意味着 $|\Gamma|\le1/3$；不超过1.5意味着 $|\Gamma|\le0.2$。这是单器件指定端接条件下的指标，不意味着多个器件串联后仍自动满足相同VSWR。

## 2.2 必须分别统计的功率

对于单板，应分别报告直通功率、回波功率、天线取能支路接受功率、指定工件体积内吸收功率、铜损、FR4损耗、PP损耗、触点/识别电路损耗及非目标辐射功率。

令 $P_{\rm tap}$ 为取能支路净接受功率，$P_{\rm work}$ 为指定工件内实际吸收功率。用

$$
\eta_h=\frac{P_{\rm work}}{P_{\rm tap}}
$$

描述该局部等效链的有效加热效率时，必须明确其测量边界。空间强互耦条件下，不同天线对同一工件区域的“单独贡献”含干涉项，不能随意按孤立天线效率分摊；这时直接以工件分区吸收积分定义验收功率更稳妥。

只知道两端口S参数时，

$$
1-|S_{11}|^2-|S_{21}|^2
$$

是进入所有未显式输出通道的功率比例，可能包含辐射和材料损耗，**并不等于工件加热效率**。

## 2.3 直通衰减不等于寄生损耗

定义 $r=|S_{11}|^2$，$t=|S_{21}|^2$，$c=P_{\rm tap}/P_{\rm in}$，$\ell$ 为其余损耗比例。在一致边界、单侧激励及相应通道完整计入时，

$$
r+t+c+\ell=1.
$$

有用取能本身也会降低直通功率。报告应同时给出 $IL_{\rm through}=-10\log_{10}t$ 与寄生比例 $\ell$，不要通过重新命名把实际抽取能量从传统S参数插损定义中“抹去”。若采购合同要限制的是寄生损耗，应明确改名并定义测量方法，不能继续含混地使用“直通插损”。

## 2.4 组内功率均匀性

令

$$
\bar p=\frac1N\sum_{i=1}^{N}p_i,\qquad
U_\infty=\max_i\left|\frac{p_i-\bar p}{\bar p}\right|.
$$

本报告将“偏差不超过±25%”定义为 $U_\infty\le0.25$。同时独立检查所有板的 $5\le p_i\le8$ W。不能只检查标准差、最大最小比或组总功率。

若对设定值6.5 W采用±25%，允许区间为4.875至8.125 W，并不保证落入5至8 W。要保证围绕6.5 W的对称误差区间完全处于硬范围内，误差应不超过 $1.5/6.5\approx23.08\%$。

# 3. 原始需求的严格可行性检验

## 3.1 定理一：总功率守恒给出无条件必要条件

**假设：** 系统无额外有源供能，在周期稳态或长时间平均意义下运行；所有工件、连接件和终端均为被动系统；500 W指进入评估系统的最大前向源功率。

对包含源输出参考面以外全部结构的控制体积分坡印廷定理，并对周期取平均，储能变化项消失，得到

$$
P_s=P_{\rm reflected}+P_{\rm feed}+P_{\rm parasitic}
+P_T+P_{\rm escape}+\sum_{i=1}^{N}p_i.
$$

各项非负，因此

$$
P_s\ge\sum_{i=1}^{N}p_i\ge Np_{\min}.
$$

对 $N=100$、$p_{\min}=5$ W，右侧等于500 W。若 $P_s\le500$ W，则必须同时有每板恰好5 W，且所有其他项严格为零。只要电缆、铜、介质、触点、终端或非目标辐射有任何正消耗，原约束就不能成立。证毕。

每板8 W时，有效功率本身为800 W。时间复用可以改变“同时工作”的定义，却不能把全体板的连续平均5 W变成小于500 W的有效功率需求。能量回收可减少末端浪费，但不能突破有损被动系统的总能量守恒。

## 3.2 定理二：0.5 dB直通指标产生额外末端功率下界

**附加假设：** 板间为匹配单向行波、没有被忽略的空间供能通道；每板的0.5 dB指在实际取能状态下的直通功率衰减；终端为匹配负载。此处结论是该模型的下界，不冒充任意强互耦网络的一般定理。

令

$$
t_{\min}=10^{-0.5/10}=0.8912509381.
$$

末板入口功率为 $P_N$。即使其反射和寄生损耗均为零，仍有

$$
p_N\le(1-t_{\min})P_N.
$$

每板至少5 W意味着

$$
P_N\ge\frac5{1-t_{\min}}=45.9774\ {\rm W},
$$

所以末端剩余功率至少

$$
P_T\ge\frac{5t_{\min}}{1-t_{\min}}=40.9774\ {\rm W}.
$$

对100块，板链入口必须满足

$$
\boxed{P_{\rm chain}\ge100\times5+40.9774=540.9774\ {\rm W}.}
$$

这一结果甚至尚未加入线缆、桥、铜和介质损耗。相同模型下，500 W最多允许

$$
N\le\left\lfloor\frac{500}{5}-\frac{t_{\min}}{1-t_{\min}}\right\rfloor=91.
$$

91只是理想上限，绝不是实物保证数量。若要求最后一板吸收全部剩余功率，其 $S_{21}$ 应接近零，此时不能同时要求它满足0.5 dB直通插损；应把末端板另列为不同功能件。

## 3.3 定理三：单纯改变主机功率不能改善线性系统的归一化均匀性

固定频率、几何、材料和端接的线性系统中，源复幅度乘以 $\alpha$，各处电磁场也乘以 $\alpha$。因此任一被动区域的吸收功率满足

$$
p_i(\alpha)=|\alpha|^2p_i(1).
$$

于是

$$
\frac{p_i(\alpha)}{p_j(\alpha)}=\frac{p_i(1)}{p_j(1)},\qquad
U_\infty(\alpha)=U_\infty(1).
$$

主机根据板数改变总功率，能调节总体加热水平，不能自行校正已有的功率分配形状。该结论不要求板间没有反射。若利用温度非线性、可调器件、频率扫描或有源调节改变分配，则已改变模型条件，应重新分析，不能继续把它视为仅调源功率。

## 3.4 相同固定耦合板：没有失配仍会不均匀

考虑更简单而有利的情形：相同板、完全匹配、无寄生损耗，每板取走入口行波功率的 $\kappa$。则

$$
P_{i+1}=(1-\kappa)P_i,\qquad
p_i=\kappa P_s(1-\kappa)^{i-1}.
$$

取 $P_s=500$ W、$\kappa=0.01$，本次计算如下。

| 板数N | 首板/W | 末板/W | 平均/W | 最大相对偏差 | 总取能/W |
|:--|:--|:--|:--|:--|:--|
| 1 | 5.000 | 5.000 | 5.000 | 0.00% | 5.000 |
| 5 | 5.000 | 4.803 | 4.901 | 2.02% | 24.505 |
| 25 | 5.000 | 3.928 | 4.444 | 12.52% | 111.089 |
| 100 | 5.000 | 1.849 | 3.170 | 57.74% | 316.984 |

即使5块时相对均匀性很好，第5块仍小于5 W；25块时±25%均匀性可通过，但单板硬下限仍不通过。这说明“均匀”和“足够功率”必须分别验收。100块时首板5 W、末板约1.849 W，相对平均值的最大偏差达57.74%。

![图1：相同耦合比例的理想匹配级联链。数据由解析式计算，不是实际灰板全波结果。](figures/identical_chain.png){width=92%}

进一步，末板功率函数为

$$
f(\kappa)=P_s\kappa(1-\kappa)^{N-1}.
$$

求导得

$$
f'(\kappa)=P_s(1-\kappa)^{N-2}(1-N\kappa).
$$

最大值在 $\kappa=1/N$ 取得。因此100块相同理想板在500 W入口下，末板功率无论如何选择固定耦合比例，都满足

$$
\boxed{p_{100}\le5(0.99)^{99}=1.84865\ {\rm W}.}
$$

这不是“失配优化不够好”，而是此单向固定比例分配机制的限制。

## 3.5 均匀性限制与总提取效率之间的约束

若每级还含残余行波传输效率 $\tau$，定义 $q=\tau(1-\kappa)$。则 $p_i=p_1q^{i-1}$。均匀性不超过±25%的必要条件为

$$
\frac{p_N}{p_1}=q^{N-1}\ge\frac{0.75}{1.25}=0.6.
$$

对100块，$q\ge0.9948534$。这里的首末比条件仅是必要条件；相对平均值的精确指标应使用

$$
\frac{p_1}{\bar p}=\frac{N(1-q)}{1-q^N},\qquad
\frac{p_N}{\bar p}=\frac{N(1-q)q^{N-1}}{1-q^N}.
$$

在完全无损、100块、500 W的算例中，求解精确 $U_\infty=0.25$ 得到 $\kappa\approx0.00468199$。此时首板2.341 W、末板1.471 W、平均1.873 W，总取能187.279 W，末端仍余312.721 W。为保持均匀而选很弱的同一耦合，反而会牺牲整体提取效率。

## 3.6 三端口理想分配器的结构限制

不能把“直通端、输入端、取能端”简单设成同时无损、互易且三个端口均匹配的理想三端口。若满足这些条件，则其S矩阵应为

$$
S=\begin{pmatrix}0&a&b\\a&0&c\\b&c&0\end{pmatrix},\qquad S^\dagger S=I.
$$

列正交要求 $b^*c=0$、$a^*c=0$、$a^*b=0$。非平凡功分需要同一输入同时向两个输出传能，与这些等式矛盾。因此实现必须引入第四端口、隔离耗散、非互易结构或接受某些端口失配。实际定向耦合器的隔离端及其负载，不能在功率和热预算中被省略。

# 4. 三维电磁与电热问题的数学模型

## 4.1 控制方程和材料约定

采用 $e^{j\omega t}$ 峰值相量约定。将介电损耗写为 $\varepsilon_r^c=\varepsilon_r'-j\varepsilon_r''$，把独立导电损耗写为 $\sigma$。电场满足

$$
\nabla\times\left(\mu_r^{-1}\nabla\times\mathbf E\right)
-k_0^2\left(\varepsilon_r^c-j\frac{\sigma}{\omega\varepsilon_0}\right)\mathbf E
=-j\omega\mu_0\mathbf J_s,
\qquad k_0=\omega\sqrt{\mu_0\varepsilon_0}.
$$

已经包括在 $\varepsilon_r''$ 内的导电损耗不得再次写入 $\sigma$。金属、磁铁和有损介质应根据实际模型采用表面阻抗、体电导率或复磁导率，不得把所有物体统一成PEC或无损材料。

标准频域电磁与热方程耦合的基本做法，是把电磁损耗作为热源；其时间尺度假设是电磁周期远短于热演化时间。[R5]

## 4.2 弱形式与离散

在允许的 $H(\mathrm{curl})$ 试验空间中，取向量测试函数 $\mathbf v$，得到

$$
\int_\Omega (\nabla\times\mathbf v)^*\cdot\mu_r^{-1}(\nabla\times\mathbf E)\,dV
-k_0^2\int_\Omega\mathbf v^*\cdot\varepsilon_{r,\mathrm{eff}}^c\mathbf E\,dV
+B_{\partial\Omega}(\mathbf v,\mathbf E)
=-j\omega\mu_0\int_\Omega\mathbf v^*\cdot\mathbf J_s\,dV.
$$

其中 $B_{\partial\Omega}$ 包含端口、阻抗和开放边界贡献。实际有限元应采用与旋度空间相容的边元离散，并通过网格、边界距离、端口模式和能量收支检查解的可靠性。频域Maxwell问题存在谐振和材料条件相关性，不能仅凭一个通用网格尺寸声称获得了严格全局误差保证。

## 4.3 功率积分

在非磁性工件内，时间平均体热源为

$$
q_{\rm EM}=\frac12\left(\sigma+\omega\varepsilon_0\varepsilon_r''\right)|\mathbf E|^2.
$$

若磁性损耗适用，还需加入 $\tfrac12\omega\mu_0\mu_r''|\mathbf H|^2$。金属表面阻抗近似下，

$$
P_{\rm Cu}=\frac12\int_{S_{\rm Cu}}\operatorname{Re}(Z_s)|\mathbf H_t|^2\,dS.
$$

对第 $i$ 个明确划分的工件区域，

$$
p_i=\int_{V_i}q_{\rm EM}\,dV.
$$

通过给定表面的净功率使用

$$
P_{\Sigma}=\frac12\operatorname{Re}\int_\Sigma
(\mathbf E\times\mathbf H^*)\cdot\mathbf n\,dS.
$$

在3 cm近区不应直接以平面波的 $|E|^2/(2\eta_0)$ 代替该式。

## 4.4 归一化与温度迭代

在线性、温度固定、同一源参考面下，以1 W前向功率计算的场可按

$$
\mathbf E(P)=\sqrt{P/1\mathrm{W}}\,\mathbf E_{1\mathrm W}
$$

缩放，热源随P线性缩放。若1 W定义为“接受功率”而500 W定义为“前向功率”，应先通过实际反射系数转换，不能直接开方相乘。

温度改变材料、接触电阻或工件介电特性后，应重新求电磁场。不能把室温小信号解一直外推到明显升温的500 W稳态。

# 5. 本次实际完成的二维准静态截面计算

## 5.1 为什么需要这一步

“能量线宽2 mm”没有独立的阻抗意义：阻抗取决于线宽、导体到地距离、上下介质、铜厚和邻近导体。传统空气上覆盖的薄微带公式只能作为基线；覆盖PP后需要重新计算。微带模型中的准静态近似、频散及导体/介质损耗是不同层次的问题，不能混称为三维全波结果。[R3][R4]

本次不只列出公式，而是实际求解了用户层叠对应的一个明确简化截面。

## 5.2 二维边值问题及唯一性

设截面电势为 $\phi(x,y)$。求解

$$
\nabla_t\cdot(\varepsilon\nabla_t\phi)=0,
$$

导体带设为1 V，底部地和有限计算域的外侧边界为0 V。FR4位于 $0<y<1.6$ mm，PP位于 $1.6<y<3.6$ mm，上方为空气。基准域宽40 mm、高15 mm。

相应Dirichlet能量为

$$
\mathcal E[\phi]=\frac12\int_\Omega\varepsilon|\nabla_t\phi|^2\,dA.
$$

当各区 $\varepsilon>0$ 且Dirichlet边界非空时，该泛函对自由电势严格凸。若两解差为u，则分部积分得到 $\int\varepsilon|\nabla u|^2dA=0$，且u在Dirichlet边界为零，从而u恒为零。故该截面边值问题有唯一的有限能量解。

本次有限体积离散把相邻节点间的介质加权通量装配成稀疏对称正定系统。材料界面上的通量系数按对应双控制面内的介质占比处理。求解后同时以导体电荷与电场能量计算电容，二者的一致性作为实现核查，而不是单凭一张电势图判断正确。

## 5.3 电容、特性阻抗和能量参与率

单位长度电容为

$$
C=\frac{1}{V^2}\int_\Omega\varepsilon|\nabla\phi|^2dA.
$$

保持同一几何、把介质全部换成真空得到 $C_0$。非磁性准TEM近似中，

$$
L=\frac1{c_0^2C_0},\qquad
\varepsilon_{\rm eff}=\frac C{C_0},\qquad
Z_0=\sqrt{\frac LC}=\frac1{c_0\sqrt{CC_0}}.
$$

材料m的电场能量参与率定义为

$$
F_m=\frac{\int_{\Omega_m}\varepsilon|\nabla\phi|^2dA}
{\int_\Omega\varepsilon|\nabla\phi|^2dA}.
$$

小损耗扰动近似下，

$$
\tan\delta_{\rm eff}=\sum_mF_m\tan\delta_m,
\qquad\alpha_d\approx\frac\beta2\tan\delta_{\rm eff},
\qquad IL_d=\frac{20}{\ln10}\alpha_d L_{\rm line}.
$$

这里的 $\alpha_d$ 是场幅衰减系数，单位Np/m；$L_{\rm line}$ 是传输线长度，不是单位长度电感。

## 5.4 网格加密与域尺寸检查：实际数值

以下均为2 mm导体宽度、2 mm PP直接覆盖的结果。

| 网格/mm | 自由未知量 | C/(pF/m) | 阻抗/Ω | 有效介电常数 | 介质损耗/(dB/100mm) |
|:--|:--|:--|:--|:--|:--|
| 0.2 | 14715 | 109.031 | 57.744 | 3.5624 | 0.6791 |
| 0.1 | 59430 | 107.057 | 58.775 | 3.5584 | 0.6769 |
| 0.05 | 238860 | 106.080 | 59.299 | 3.5564 | 0.6759 |

0.1 mm到0.05 mm网格的阻抗变化约0.89%，介质损耗变化约0.15%。在0.1 mm网格上，将计算域从40 mm×15 mm扩大到60 mm×25 mm，阻抗由58.775 Ω变为59.016 Ω，约变化0.41%。这些数值支持截面阻抗约59至60 Ω的初步判断，但不构成真实三维结构的验收误差界。

有限域外边界接地是开放截面的数值近似，零厚度铜边缘也会产生离散误差。本次没有计算真实35 μm铜截面、铜面粗糙度、GHz频散、识别线、触点或线圈；所以不能用59.299 Ω这一小数值宣称实物阻抗已精确确定。

## 5.5 线宽扫描：实际数值

| 线宽/mm | 阻抗/Ω | 有效介电常数 | FR4能量参与率 | 介质损耗/(dB/100mm) |
|:--|:--|:--|:--|:--|
| 1.5 | 67.956 | 3.5274 | 0.7888 | 0.6607 |
| 2.0 | 59.299 | 3.5564 | 0.8036 | 0.6759 |
| 2.5 | 52.806 | 3.5824 | 0.8165 | 0.6893 |
| 3.0 | 47.700 | 3.6058 | 0.8278 | 0.7011 |

在该简化层叠中，50 Ω位于2.5与3.0 mm两种计算宽度之间；可把约2.7至2.9 mm作为后续二维/三维细扫的起点，而不是加工定值。铜厚、胶层及实际外壳变化后，应重新确定线宽。

作为对照，空气上覆盖的常用薄微带近似在2 mm宽、1.6 mm厚、$\varepsilon_r=4.4$ 时给出约63.56 Ω；本次空气上覆盖的有限域0.05 mm网格计算约62.54 Ω。两种近似的一致量级可用于代码合理性检查，但不是二者都“已收敛到真值”的证明。

## 5.6 介质损耗对桥指标的意义

在2 mm宽、PP覆盖、0.05 mm网格结果中，FR4能量参与率约0.8036。取示例 $\tan\delta_{\rm FR4}=0.02$，PP损耗暂为零，在2.45 GHz下仅FR4导致的100 mm路径损耗约0.6759 dB。

这已经超过平面桥0.2 dB与立体桥0.3 dB指标，尚未计入铜、触点、回流不连续及辐射。它是**对该假设截面和材料的否决证据**，不是对所有名为FR4的材料和所有可能桥形的普遍不可能定理。

仅给出“FR4、介电常数4.4”不足以确定损耗。作为材料差异的实例，Isola 370HR数据表给出2 GHz处Dk约4.04、Df约0.021；该例证明不能把FR4统一当作固定实介电常数的无损介质，但不能把该牌号参数直接指定给未确认的项目板材。[R2]

在上述参与率不变的近似下，要把100 mm介质损耗压至0.2 dB，需

$$
\tan\delta_{\rm FR4}\lesssim0.02\frac{0.2}{0.6759}\approx0.00592.
$$

这是把整个插损额度都给介质的乐观上限；还要为铜、连接和失配留预算，因此实际材料门槛应更严。由此应把“基材牌号/传输线截面”提升为一级优化变量，而不是只调耦合段长度。

# 6. 包含多次反射的级联网络计算

## 6.1 每个模块的端口模型

对第i个两端口模块，

$$
\mathbf b_i=S_i\mathbf a_i.
$$

模块可能是灰板、桥、磁吸转接或电缆。S矩阵必须保留复幅值与相位，不能只保存dB曲线。对于两端匹配的均匀同轴线，可用 $S_{11}=S_{22}=0$、$S_{21}=S_{12}=e^{-(\alpha+j\beta)L}$；其中幅度衰减 $\alpha=(\ln10/20)\alpha_{\rm dB/m}$，相位常数 $\beta=2\pi f/v_p$，传播速度 $v_p$ 取具体线缆数据或测量值。仅用插损、把相位固定为零会破坏整链反射计算。对于有取能支路的灰板，宜先建三端口或四端口结构，并以真实天线/工件负载端接后再缩减。

将端口分为外部e与负载l，若负载反射矩阵为 $\Gamma_L$，由 $a_l=\Gamma_Lb_l$ 消去内部端口，得

$$
S_{\rm red}=S_{ee}+S_{el}(I-\Gamma_LS_{ll})^{-1}\Gamma_LS_{le}.
$$

因此工件负载改变，会改变模块的等效两端口S参数。不能把空载天线模型一直用于所有湿度、温度和距离。

## 6.2 整链波方程

将所有模块组成块对角矩阵 $S_D$，用连接矩阵C描述相邻端口的反向关联：一个模块右端的出射波成为下一个模块左端的入射波，反向亦然。加上外部源向量d，

$$
\mathbf a=C\mathbf b+\mathbf d,\qquad
\mathbf b=S_D\mathbf a.
$$

因此

$$
\boxed{(I-CS_D)\mathbf a=\mathbf d.}
$$

求解该系统即可保留所有沿连接网络传播的多次反射。本文附件的 `solve_chain` 使用该形式，校验输入S矩阵被动性，并输出各模块入射波、出射波、净吸收功率、源反射、终端功率及能量残差。

这里“所有反射”限于输入模型已经包含的连接图。若100块板之间存在未建模的自由空间互耦，矩阵算法再精确也不会自动产生这些耦合。

## 6.3 被动性与功率二次型

对实正参考阻抗归一化的被动网络，

$$
D_i=I-S_i^\dagger S_i\succeq0.
$$

模块净吸收/非显式输出功率为

$$
P_{i,\rm net}=\mathbf a_i^\dagger D_i\mathbf a_i.
$$

应再从场积分提取 $Q_{i,\rm work}$、$Q_{i,\rm Cu}$、$Q_{i,\rm FR4}$ 等矩阵，使总功率分解可核验。若只能获得S2P文件，就只能确定总的D矩阵，不能唯一识别其中多少是工件热、多少是非目标辐射。

对完整多端口的线性场响应，任一有损区域V对应

$$
(Q_V)_{mn}=\frac12\int_V
(\sigma+\omega\varepsilon_0\varepsilon_r'')\,
\mathbf E_m^*\cdot\mathbf E_n\,dV,
$$

从而 $P_V=\mathbf a^\dagger Q_V\mathbf a$。交叉项保留相干干涉，且 $Q_V\succeq0$。

## 6.4 本次程序校验结果

附件执行了理想匹配100级网络，以及带复数并联导纳、负载反射系数幅度0.2的5/25/100级合成网络测试。后一类仅是用于检验算法的被动电路，不是灰板几何模型。

| 测试模型 | 反射/W | 终端/W | 各模块净吸收合计/W | 能量残差/W |
|:--|:--|:--|:--|:--|
| 匹配100级 | 0.000000 | 183.016171 | 316.983829 | -1.08e-12 |
| 合成并联模型N=5 | 24.082920 | 437.183565 | 38.733514 | 2.98e-13 |
| 合成并联模型N=25 | 15.322543 | 329.397251 | 155.280206 | 5.97e-13 |
| 合成并联模型N=100 | 1.264796 | 112.935016 | 385.800189 | -2.27e-13 |

匹配100级求解得到的每级功率与解析几何级数逐点一致；所有测试的总功率平衡最大绝对残差约 $1.1\times10^{-12}$ W。该结果证明这些测试范围内的网络联接和功率记账实现一致，**不证明项目几何已经满足S参数、场均匀性或温升要求**。

## 6.5 小反射为何也要保留相位

弱反射一阶近似中，入口反射可写成各级反射经往返相位及传播衰减加权后的复数和。某些间距使反射相消，另一些使其相加。不能把反射损耗直接逐级做标量相加，也不能假设总反射必然按平方根增长。

空间排布、桥长、同轴长度和工件负载变化都可能改变相位关系。单板VSWR通过不等于整链通过，5块通过也不等于25或100块通过。

# 7. 能够严格反算的均匀取能结构

## 7.1 定理四：位置相关耦合的构造

设第i板目标有效功率为p，局部有效效率为 $\eta_i$，板后剩余传输效率为 $\tau_i$。在匹配单向模型内，定义板入口功率 $P_i$、下一板入口功率 $P_{i+1}$，则

$$
P_{i+1}=\tau_i\left(P_i-\frac p{\eta_i}\right).
$$

给定末端剩余功率 $P_{N+1}=P_T\ge0$，从后往前递推

$$
\boxed{P_i=\frac p{\eta_i}+\frac{P_{i+1}}{\tau_i}},
\qquad
\boxed{\kappa_i=\frac{p}{\eta_iP_i}}.
$$

若 $0<\tau_i\le1$、$0<\eta_i\le1$ 且 $P_T>0$，则 $P_i>p/\eta_i$，故 $0<\kappa_i<1$。把此耦合代回递推，立刻得到每板有效功率严格等于p。因此该分配在此理想模型内存在，并可构造。证毕。

该定理解决的是“应该抽取多少比例”，不解决“5至15 mm耦合段能否实现此比例”。几何可实现性、带宽、隔离及工件负载仍需电磁模型验证。

## 7.2 常系数闭式解

若各级 $\eta_i=\eta$、$\tau_i=\tau$，令 $d=p/\eta$，则

$$
P_1=d\sum_{j=0}^{N-1}\tau^{-j}+P_T\tau^{-N}.
$$

当 $\tau=1$ 时，$P_1=Nd+P_T$；当 $\tau<1$ 时，

$$
P_1=d\frac{\tau^{-N}-1}{\tau^{-1}-1}+P_T\tau^{-N}.
$$

若前馈电缆/转接总传输效率为 $t_f$，源端需求为 $P_s=P_1/t_f$。铜、介质和接触损耗的总和并非简单的“单板损耗瓦数乘N”，因为各级通过的功率不同；必须按各级实际功率加权。

## 7.3 本次反算例：5 W每板

采用每板5 W、$\eta=1$、每级寄生损耗0.02 dB、末端50 W、入口馈线0.5 dB。**0.02 dB是用于展示分配规律的乐观假设，不是普通FR4或本次三维模型的实测结果。**

| 板数N | 板链入口/W | 源端需求/W | 链内寄生损耗/W | 末板直通插损/dB |
|:--|:--|:--|:--|:--|
| 1 | 55.231 | 61.970 | 0.231 | 0.4321 |
| 5 | 76.397 | 85.718 | 1.397 | 0.4321 |
| 25 | 188.276 | 211.249 | 13.276 | 0.4321 |
| 100 | 712.823 | 799.801 | 162.823 | 0.4321 |

100块需要板链入口约712.82 W、源端约799.80 W。其中有效取能500 W，级内寄生损耗约162.82 W，末端50 W；馈线损耗另计。即使每级只有0.02 dB，100级高功率通道的累计消耗也不可忽略。

100块的部分位置反算结果如下。

| 板序号 | 入口功率/W | 取能比例 | 有用取能/W | 该级寄生热/W |
|:--|:--|:--|:--|:--|
| 1 | 712.823 | 0.7014% | 5.000 | 3.252 |
| 25 | 524.892 | 0.9526% | 5.000 | 2.389 |
| 50 | 350.009 | 1.4285% | 5.000 | 1.585 |
| 75 | 194.145 | 2.5754% | 5.000 | 0.869 |
| 100 | 55.231 | 9.0529% | 5.000 | 0.231 |

耦合比例由首板约0.701%增加至末板约9.053%。N改变时，相同位置需要的耦合也改变：5块和25块首板分别约6.545%和2.656%。所以一套完全相同且不可调的固定板，不能直接等同于这套按位置和总板数合成的分配。

![图2：均匀5 W取能模型所需源端功率。曲线基于给定假设，不是实物性能承诺。](figures/required_source_power.png){width=92%}

![图3：按板数与位置反算的耦合比例。图示为目标电气参数，不是最终加工尺寸。](figures/graded_coupling.png){width=92%}

## 7.4 500 W条件下可支持的板数：有条件算例

维持5 W、末端50 W、馈线0.5 dB和理想有效效率1，本次枚举N=1至100得到：每级寄生0、0.02、0.1、0.3 dB时，500 W可支持的最大N分别为79、65、39、21。

这些是指定模型内的数值，不是对实际设备的推荐上限。尤其0.02 dB并未由FR4结构证明。若坚持100块与其他上述比例不变，源端500 W对应的等比例单板功率只有约3.13 W，不能声称满足每块至少5 W。

## 7.5 可实施架构的选择边界

**位置分级的被动耦合板：** 按序号制造不同耦合档位；优点是控制简单，缺点是不能任意交换位置，也不能对所有N共用同一分级表。

**通用板加可调耦合：** 保留同一机械板，增加经高功率验证的可调网络、局部检测和位置/板数信息。可调器件的耐压、插损、隔离、非线性和故障状态成为新的设计约束；不能把控制算法当作零损耗硬件。

**多支路/分区供能：** 减少每条串联链的长度和动态范围，但功分器、馈线和末端负载仍要记账。把100块拆成4组25块，并不会使共用500 W源能够无损提供100×5 W。多个独立源或提高总功率属于明确的系统规格变更。

没有一种结构能绕过第3.1节的总功率约束。选择架构应比较真实损耗、末端负载数量、可调范围和维护复杂度，而不是先宣称某一种必然最优。

# 8. 灰板天线和耦合段的设计方法

## 8.1 必须把“线圈”作为分布式结构建模

在2.45 GHz算例中，自由空间波长约122.36 mm。本次PP覆盖截面 $\varepsilon_{\rm eff}\approx3.56$，对应准TEM导波波长约64.9 mm。50 mm板长已经是显著电长度；方形螺旋总导体路径可能更长。

因此不能仅用集中电感 $L\propto n^2$ 推断2至4圈线圈的GHz行为。线圈外径、内径、匝间距、开口、接地方式、PP覆盖和工件位置均影响分布电容、谐振和电流分布。背面整片接地也必须进入模型，不能直接套用自由空间小环天线公式。

## 8.2 耦合量的电气目标

均匀取能反算首先给出目标 $\kappa_i(f_0)$。几何优化才进一步寻找满足它的耦合长度、间距、线宽和负载匹配。对于理想对称、等相速度、四分之一波长耦合线，设电压耦合系数为K，则

$$
K=\frac{Z_{0e}-Z_{0o}}{Z_{0e}+Z_{0o}},\qquad\kappa=K^2,
$$

$$
Z_{0e}=Z_0\sqrt{\frac{1+K}{1-K}},\qquad
Z_{0o}=Z_0\sqrt{\frac{1-K}{1+K}}.
$$

这是理想耦合线初值关系；覆盖层、非等相速度、短耦合段和有损负载会使关系偏离。以本次导波波长计算，四分之一波长约16.2 mm，略超原始长度上限15 mm。因此原始5至15 mm范围不必然包含标准四分之一波长解；可能需要电容加载、不同截面或非四分之一波长设计，并重新评估带宽和峰值电场。

## 8.3 由3 cm场/热目标反向约束天线

优化不能仅以 $|S_{31}|^2$ 达到5 W为目标。对于相同取能功率，不同线圈模式可能使能量主要损耗于板材、向侧面辐射或在工件边缘集中。

建议采用层级约束：先满足源与功率预算；再满足所需耦合与寄生损耗；然后最大化工件区域吸收并限制非目标辐射；最后在此可行域内优化近场、温升和局部场强。每一步都应输出可追溯的功率分账，而非只输出一个漂亮的驻波比。

## 8.4 参数空间扩充

除原始四个变量外，应纳入：线圈外径/内径/匝间距、耦合线阻抗、局部匹配结构、材料牌号、气隙、导体边缘圆角、回流过孔和接地接触、识别线终端、负载距离及终端处理。

可固定变量必须有冻结依据；例如外形50 mm×50 mm可以先固定，但“FR4一定不变、2 mm能量线一定不变”会限制可行性，应由第5节和损耗预算决定是否保留。

# 9. 平面桥、立体桥、磁吸触点与连接线

## 9.1 平面桥的电气结构

桥应看作“输入接触区 + 受控阻抗传输线 + 输出接触区”，背面地必须通过低阻抗路径跨接到相邻板地。仅把正面能量线连通，而让背面地通过未定义的空气电容或识别细线返回，会改变传输模式并增加回流电感。

在原清单没有地触点定义的情况下，不能直接断言实物完全无地；但必须要求补充实际地回路剖面和接触方式。可研究独立地弹片、宽面地接触、围绕信号的地结构或经验证的电容式回流，不可默认磁铁同时是可靠的射频地。

## 9.2 立体直角桥不是单纯的平面拐角

R3至R5只规定了局部圆角，不完整规定三维90°转折的电气路径。必须明确采用刚柔板、两块刚板焊接、金属中继片还是其他结构，并把信号线和回流面的三维转折一起建模。

两个50 mm边长不必然等于100 mm铜电长度；实际转折、搭接与接触区会改变路径。圆角可能减小几何突变，却不能抵消材料损耗或断开的回流。立体桥应单独计算弯头电流拥挤、模态转换和拐角附近工件耦合。

## 9.3 从驻波比反推允许串联寄生

用串联阻抗 $Z_s$ 连接两条50 Ω理想线，其

$$
S_{21}=\frac{2Z_0}{2Z_0+Z_s},\qquad
S_{11}=\frac{Z_s}{2Z_0+Z_s}.
$$

若 $Z_s=j\omega L_s$，要求VSWR不超过1.5，即 $|S_{11}|\le0.2$，则

$$
\omega L_s\le\frac{2Z_0\times0.2}{\sqrt{1-0.2^2}}=20.412\ \Omega.
$$

2.45 GHz下得到 $L_s\lesssim1.326$ nH。这是把整个失配额度都分配给一个串联电感的乐观上限。真实结构还含两端触点、电容突变和转折，所以单接触区所允许的寄生通常更少。该推导也说明“接触面积不小”不能替代高频回流设计。

## 9.4 连接桥的插损与驻波比必须联合验收

平面桥若同时达到边界值 $IL=0.2$ dB、$VSWR=1.5$，则

$$
|S_{21}|^2=0.954993,\quad |S_{11}|^2=0.04,
$$

留给非端口输出的损耗比例仅约0.005007，即入口功率的0.5%。不能一边把0.2 dB全部分配给材料，一边又把VSWR1.5的反射当作额外免费额度。

需提供所有端口组合和极限装配下的复数S参数，先检查功率可实现性，再检查各单项限值。

## 9.5 RG142型号的已核查数据与推算

本报告核查了HUBER+SUHNER的RG_142_B/U厂家数据表，文档DOC-0000177741 AN，发布于2019年7月22日。表中给出的衰减公式和连续波功率公式为

$$
\alpha(f)=0.3956\sqrt f+0.0645f\quad[\mathrm{dB/m}],
\qquad P_{\rm CW}(f)=\frac{407}{\sqrt f}\quad[\mathrm W],
$$

其中f以GHz计。衰减标称条件为海平面25 ℃，连续波功率条件为海平面40 ℃；二者不能混为同一温度工况。[R1]

| 频率算例 | 衰减/(dB/m) | 3m本体/dB | 5m本体/dB | 40℃条件CW算例/W |
|:--|:--|:--|:--|:--|
| 0.915 GHz | 0.4374 | 1.312 | 2.187 | 425.5 |
| 2.45 GHz | 0.7772 | 2.332 | 3.886 | 260.0 |

这些值针对核查的具体型号，不代表所有供应商RG142都完全相同；采购时仍需确认当前具体料号及版本。但是，这个厂家实例在两个示例频率下都明显达不到5 m、0.5 dB，也不能仅凭RG142名称证明其具备500 W连续波容量。

![图4：核查型号RG142的本体衰减与原连接线总成限值对比。](figures/rg142_loss.png){width=92%}

对于匹配、被动的工程预算，连接线总成至少要分解为

$$
IL_{\rm assy}\approx\alpha L+IL_{N}+IL_{\rm launch}+IL_{\rm magnetic}.
$$

该式用于低反射、匹配级联预算；高反射时应通过完整网络求解，不能机械相加。优化过渡结构可以减少其自身损耗和失配，不能消除有损电缆传播过程中的耗散。

若5 m总成仅允许0.5 dB，而先为两端过渡预留0.1 dB，则候选电缆本体应满足 $\alpha\le0.08$ dB/m，并且在实际温度、弯曲、占空比和驻波比下具备足够功率额定值。不要仅因候选电缆名称带“低损耗”便认定符合此门槛。

## 9.6 RG142的500 W算术例不能作为运行许可

按2.45 GHz、5 m标称衰减把500 W代入，数学上末端约204.34 W，差额约295.66 W。这个数字只是说明损耗量级；输入500 W已高于上述型号在相应频率的40 ℃连续波额定算例，不能据此建议实际通电验证。

容量校核必须分别覆盖额定CW热限制、脉冲峰值电压、连接器降额、弯曲与安装散热。满足50 Ω或某个低频耐压指标，不等于满足GHz下500 W连续波能力。

# 10. 近场与真实加热均匀性

## 10.1 3 cm评估平面的定义

建议把前PP外表面定义为z=0，向工件方向为正，在z=30 mm设评估面。若实际CAD把铜面作为z=0，应把2 mm PP、气隙及胶层厚度计入，不能把“距铜30 mm”误作“距外壳30 mm”。

评估区域应提供多边形边界及采样规则：是单板投影50 mm×50 mm、全阵列覆盖区、排除桥的区域，还是工件实际轮廓。应固定边缘是否包含，不能在看到结果后裁掉热点或低场区。

## 10.2 场幅均匀性定义

取评估区域A内的归一化参考场

$$
E_{\rm ref}=\sqrt{\frac1{|A|}\int_A|\mathbf E(\mathbf r)|^2\,dA},
$$

并定义

$$
\Delta_E(\mathbf r)=20\log_{10}\frac{|\mathbf E(\mathbf r)|}{E_{\rm ref}}.
$$

“±5 dB”可冻结为所有规定采样点及经空间收敛检查后的区域极值满足 $-5\le\Delta_E\le5$ dB。场幅允许约0.562至1.778倍参考值，最大最小场幅比可达3.162，对应 $|E|^2$ 最大最小比达10。

因此±5 dB是相当宽的场能量起伏，不等于温度±5%、吸收功率±5%或±25%。近场指标与功率均匀性必须分开保留。

## 10.3 不能把各板孤立场强的模直接相加

在线性系统中，

$$
\mathbf E(\mathbf r)=\sum_i a_i\mathbf E_i(\mathbf r),
$$

所以

$$
|\mathbf E|^2=\sum_i|a_i\mathbf E_i|^2+
2\operatorname{Re}\sum_{i<j}a_ia_j^*\mathbf E_i\cdot\mathbf E_j^*.
$$

交叉项可以增强，也可以抵消。即使每板取能相等，3 cm平面也可能出现明显波纹或零点；反之，单板场分布较平滑，也不保证100板叠加平滑。

因此必须保留每板激励相位和空间互耦。若采用频率扫描平均场，应明确不同频率的驻留时间、源功率以及工件热时间常数，并按能量累计，而不是把不同频率的复电场无条件相干相加。

## 10.4 实际工件吸收与温度

对于均匀材料，局部热源与 $|E|^2$ 成正比；对于空间不均匀、温度依赖或含水量变化的材料，还需使用空间与温度相关的复介电常数。

最终报告应同时给出工件分区吸收功率、吸收功率密度峰值、随时间的温度分布和温差。仅给空载空气中的E场图，不能证明微波加热任务完成。

# 11. 500 W容量、局部电场与电热稳定性

## 11.1 电压电流尺度

50 Ω线上500 W前向行波对应

$$
V_{\rm rms}=\sqrt{PZ_0}=158.11\ \mathrm V,\qquad
I_{\rm rms}=\sqrt{P/Z_0}=3.162\ \mathrm A,
$$

匹配行波的电压峰值约223.61 V。若 $|\Gamma|=1/3$，沿线电压包络最大值可达

$$
V_{\max,\rm pk}=\sqrt{2P_fZ_0}(1+|\Gamma|)=298.14\ \mathrm V.
$$

把这一电压作为某危险间隙两端电压的保守尺度，在0.3 mm间隙上得到约0.994 kV/mm，已经接近用户的1 kV/mm目标。但这是数量级估算，不是具体耦合缝隙的求解结果：两根相邻信号线的电位差并不必然等于对地峰值电压，真实场必须按实际两导体和局部谐振求解。

电压和电流驻波最大位置一般不同；不能把两者最大值当作同一点同时发生的实测状态。

## 11.2 尖角、有限曲率与打火判据

零厚度PEC尖角可出现局部场奇异性，导致网格越细报告的最大场越大。真实铜边缘圆角、蚀刻截面、毛刺、镀层和接触间隙必须进入热点子模型；不能通过粗网格或排除热点来制造“最大场低于阈值”的结论。有限元尖角奇异性与网格相关峰值的区别可参见求解器厂商的说明。[R6]

应分别报告空气间隙、固体介质和界面沿面的峰值/高梯度区域。用户1 kV/mm只是设计限制，不能替代对频率、气压、温湿度、污染、沿面爬电和热插拔电弧的实际安全验证。

## 11.3 导体损耗与接触热

本次以铜电导率 $5.8\times10^7$ S/m作示例，在2.45 GHz得到

$$
\delta=\sqrt{\frac{2}{\omega\mu_0\sigma}}\approx1.335\ \mu\mathrm m,
\qquad R_s=\sqrt{\frac{\omega\mu_0}{2\sigma}}\approx0.01291\ \Omega/\square.
$$

35 μm铜厚远大于该趋肤深度，但这不意味着零损耗；表面粗糙度、边缘电流集中、镀金下层和回流面仍会影响损耗。[R3][R4]

接触电阻的最简热尺度为 $P_c=I_{\rm rms}^2R_c$。在匹配500 W、50 Ω情形中，若实际RF等效接触电阻为10 mΩ，电阻热约0.1 W；若为100 mΩ则约1 W。这里的电阻值是说明敏感性的假设，不是弹片检测数据。必须通过接触压力、磨损、污染和温度试验得到合理范围。

## 11.4 热方程与反馈

对工件、PCB、外壳和触点，求解

$$
\rho c_p\frac{\partial T}{\partial t}=\nabla\cdot(k\nabla T)+q_{\rm EM}+q_{\rm contact}.
$$

边界包含实际对流、辐射、接触导热及可能的强制冷却。接触导热电阻、PP隔热和末端负载散热不能省略。若触点耗散已经计入电磁热源，就不得再重复加入 $q_{\rm contact}$。[R5]

建议采用交替迭代：给定温度求电磁损耗，更新温度，再按温度更新介电参数和接触参数，直至两场收敛或达到规定加热时长。温升上限应按实际料号的连续使用温度、触点要求和工件工艺冻结，而不能把FR4玻璃化温度直接当成整个组件的允许工作温度。

## 11.5 异常工况和保护边界

至少评估末端开路/短路、板或桥缺失、地接触先断、弹片半接触、误识别板数、空载、过热与异物。磁吸结构应有先建立可靠连接和识别、再使能功率的逻辑；有功率时不得把普通磁吸接头视为可安全随意拔插的接口。

本报告没有给出人员暴露或漏场的合规限值。适用法域、设备分类、屏蔽、联锁和漏场测试须由项目合规工作单独冻结；“局部场低于1 kV/mm”绝不等于人员环境安全。

# 12. 识别电路与射频路径的共存

10 kΩ直流电阻在GHz下不是一只理想纯电阻。应保留封装寄生、电阻焊盘、识别走线、屏蔽线和控制器输入的等效网络。能量线与识别线间距1 mm不能单独证明足够隔离，因为耦合还取决于平行长度、地结构及频率。

可使用独立RF端口或实际滤波终端提取耦合，报告进入识别接口的RF功率及器件电压。滤波或偏置隔离网络必须经过高功率和故障工况验证，不能仅在低频电路图中放一只理想电感即视为完成隔离。

板数编码拓扑尚未定义。若仅作一种说明性假设：N块相同10 kΩ电阻并联，则

$$
R_{\rm eq}=\frac{10\,000}{N},\qquad
R_{99}-R_{100}=\frac{10\,000}{99\times100}\approx1.0101\ \Omega.
$$

100块附近相邻板数仅约1%的等效电阻差。电阻系统性偏差、接触电阻、温漂和ADC误差都可能影响区分。若采用串联或其他编码，关系不同，不能直接使用上述公式。板数检测应有独立误差预算和异常判定，而不能作为唯一的功率安全依据。

# 13. 完整三维仿真的执行规范

## 13.1 模型层级与每层输出

| 层级 | 模型 | 必须输出 | 本次状态 |
|:--|:--|:--|:--|
| L0 | 守恒、理想行波、功率递推 | 可行域、功率预算、耦合目标 | 已计算 |
| L1 | 二维准静态截面 | C、C0、阻抗、介质参与率及网格检查 | 已计算简化截面 |
| L2 | 单灰板与单连接件三维 | 复数S、分项损耗、端口模式、热点 | 待求解 |
| L3 | 全波提取模型的网络级联 | N=1至100功率、反射、终端负载 | 算法已测试，真实模型待输入 |
| L4 | 有工件的有限阵列三维互耦 | 空间场、工件吸收、边缘效应 | 待求解 |
| L5 | 电磁-热-接触耦合 | 温升、稳定性、异常工况 | 待求解 |
| L6 | 实物校准验证 | VNA、功率计、热测量及相关性 | 待实施 |

每层必须保留输入版本和模型缩减误差。只交付L0或L3的均匀曲线不能替代L4加热证明。

## 13.2 单器件边界与端口设置

端口截面必须包含完整信号与返回导体，并位于能够明确定义传播模式的区域。参考面设在约定的实际接触界面；若采用去嵌，应保留去嵌前后结果和距离，不能把本应计入指标的触点区去掉。

开放边界使用经过距离/厚度敏感性验证的辐射边界或PML；包含实际腔体时不能用开放空气箱替代金属腔体。1 W激励的参考是前向还是接受功率须在每张结果表中一致。

## 13.3 材料输入

采用指定牌号、频率和温度下的复介电常数、热导率、热容与密度。FR4应考虑玻纤/树脂组合的有效参数及必要的各向异性；金属应考虑厚度与粗糙度；触点应保留镀层结构或校准的等效表面阻抗。

磁铁既不能默认成空气，也不能任意使用一个巨大的直流相对磁导率去代表GHz行为。应依据厂家或测量的高频有效参数建模；资料不足时作明确的敏感性包络并列为风险，而非给出伪精确的损耗值。

## 13.4 仿真组态

网络层必须覆盖每个整数N=1至100；用户列出的5/25/100只是重点组态，不等于能够证明全部板数。

三维层建议至少包括：单板、相邻双板、5板直链、含桥的5板、25板实际排布，以及100板实际排布或经过充分有限子阵验证的域分解/多区域模型。直链、蛇形、排间折返和立体拐弯是不同空间构型，应分别定义适用范围。

对于100块不能简单以无限周期单元代替有限阵列；板数有限、每板激励不同、桥和工件破坏周期性。若使用周期或子阵近似，必须给出对有限模型的误差比较。

## 13.5 扫频、负载与公差

在实际工作带宽冻结后，先宽扫定位共振与寄生模态，再在指标边界处加密。所有工作频点都应校核反射、有效功率和热点；只优化一个频点不能证明带内性能。

几何公差应包括线宽、耦合缝隙、铜边形状、PP/胶层气隙、接触压缩与偏移、板间错位和弯曲。负载工况至少包含标称工件、允许的最小/最大介电参数、距离变化、空载和规定的异常状态。

取值应来自加工和材料数据。报告可以做敏感性参数扫描，但不能把随意选取的±10%自动声明为真实制造统计分布。

# 14. 数值可信度与严格验收裕量

## 14.1 弱耦合时，普通S参数收敛阈值可能远远不够

若取能由 $A=1-|S_{11}|^2-|S_{21}|^2$ 反算，S参数绝对误差分别不超过 $\epsilon_{11}$、$\epsilon_{21}$，则

$$
|\Delta A|\le2|S_{11}|\epsilon_{11}+\epsilon_{11}^2
+2|S_{21}|\epsilon_{21}+\epsilon_{21}^2.
$$

在500 W入口、目标取能5 W时，$A\approx0.01$。若允许取能数值误差仅0.1 W，就需 $|\Delta A|\le2\times10^{-4}$。当 $|S_{21}|\approx0.995$、忽略其他误差时，对应S21幅度绝对误差量级约 $10^{-4}$。

因此，不能仅凭相邻网格的S参数变化0.01或0.005就宣称5 W提取功率已精确收敛。相邻网格变化也不是已证明的真实误差上界。应同时核查直接功率积分、取能端口净功率和全局能量收支，以避免两个接近1的数字相减放大误差。

## 14.2 建议的数值检查项目

作为执行起点，关键共振频点至少进行三档网格比较；取能与工件吸收积分的相邻细网格变化建议控制在1%至2%以内，近场极值与局部圆角场应单独细化。严格阈值由验收误差预算反推，而不是以软件默认值替代。

源反射、工件热、导体/介质热、终端和开放边界功率应闭合。仅要求“全局能量误差小于1%”不充分：500 W的1%就是5 W，已经等于单板目标。应同时给出全局绝对残差及各板局部积分误差，不可只报一个相对百分比。

S矩阵应检查被动性、互易结构应检查互易性；超过数值容差的非被动结果应排查，而不是直接截断后用于级联。插值模型还应检查频率采样与拟合误差。

## 14.3 均匀性误差的保守传递

设计算得到 $\hat p_i$，且已建立每板绝对误差界 $|p_i-\hat p_i|\le e$。则真实平均值满足 $\bar p\ge\hat{\bar p}-e$，并有

$$
U_\infty\le
\frac{\hat U_\infty\hat{\bar p}+2e}{\hat{\bar p}-e}.
$$

例如，若计算均匀性22%，每板绝对误差界为计算平均值的1%，则

$$
U_\infty\le\frac{0.22+0.02}{0.99}\approx24.24\%.
$$

这才给25%指标留下可解释的数值余量。若e只是未经证实的经验估计，此式给出的也只是条件性保证；必须把误差界的来源写清楚。

单板硬下限应采用 $\hat p_i-e\ge5$ W，硬上限采用 $\hat p_i+e\le8$ W。刚好计算为5.000 W却没有误差余量，不宜判为稳健通过。

## 14.4 制造随机性与确定性最坏角分开

令x为设计变量，u为制造、材料和装配不确定参数。鲁棒设计应明确要求

$$
\max_{u\in\mathcal U}g_j(x,u)\le0.
$$

若采用概率性合格率而非所有公差角都通过，则必须说明u的统计分布、样本独立性和置信水平。对独立同分布样本，n次全部通过时，通过率的一侧95%置信下界为 $0.05^{1/n}$。要使该下界至少99%，需 $n\ge299$。

“300个蒙特卡洛样本无失败”是某分布和模型下的统计证据，不等于所有可能装配状态都安全。高后果异常状态仍应确定性覆盖。

# 15. 优化问题、验收矩阵与交付要求

## 15.1 分层优化问题

设x包含耦合与线圈几何、基材、接触和回流结构；$u\in\mathcal U$为规定的不确定性。对每个适用的N、频率f和负载工况，最基本可行性要求为

$$
\begin{aligned}
&P_s(x,u,N,f)\le P_{s,\max},\\
&5\le p_i(x,u,N,f)\le8\quad\forall i,\\
&U_\infty(x,u,N,f)\le0.25,\\
&VSWR_{\rm chain}(x,u,N,f)\le VSWR_{\rm lim},\\
&IL_{\rm bridge}(x,u,f)\le IL_{\rm bridge,lim},\\
&\Delta_E(\mathbf r;x,u,N,f)\in[-5,5]\quad\forall\mathbf r\in A,\\
&E_{\max}(x,u,N,f)<E_{\rm lim},\quad T_{\max}(x,u,N,f)<T_{\rm lim}.
\end{aligned}
$$

各限值只有在参考面、功率和温度定义冻结后才可执行。原始500 W/100块约束使上述可行域为空，必须先变更相关指标，不能让优化器在空可行域中返回一个“最接近”的点后称其为达标。

在确认可行域非空后，可最小化源功率、寄生热和均匀性，或作多目标Pareto比较。权重、硬约束和惩罚项必须披露；不能用损耗与均匀性的加权平均值掩盖某一硬指标失败。

## 15.2 建议修订的需求表述

| 原条款 | 必须澄清或修订的内容 |
|:--|:--|
| 1至100块、各5至8 W、最大500 W | 明确是降低最大N、提高总源功率，还是修改全体同时工作的功率要求 |
| 单板直通插损不超过0.5 dB | 明确是否包含有用取能；末端全吸收板应单列 |
| 取能5至8 W | 分别定义支路接受功率与工件吸收功率 |
| ±25%均匀性 | 定义相对平均值，并独立保留单板硬功率范围 |
| 3 cm、±5 dB | 固定前表面基准、ROI、场分量/总场和归一化方法 |
| RG142、5 m不超过0.5 dB | 按具体频率重选线缆或变更长度/插损目标 |
| 每端3个触点 | 补充明确的跨板RF地回流路径和接触时序 |
| 功率容量至少500 W | 指明CW/峰值、温度、驻波、失效状态及全部串联件额定值 |
| 交付PDF和DXF | 增加可复算原始模型、S参数、CSV和版本记录 |

## 15.3 验收判定矩阵

| 项目 | 判定量 | 当前证据状态 |
|:--|:--|:--|
| 原100板、500 W、每板至少5 W | 总功率与正损耗预算 | 已证明原组合不可同时满足 |
| 单灰板实际S参数 | 工作频带复数S、端接和参考面 | 待真实三维模型 |
| 5/25/100板实际功率均匀性 | 工件/取能积分及绝对误差 | 仅有等效模型结果，未完成实物结构验证 |
| 全部N=1至100 | 每个N及允许拓扑的验证记录 | 需输入真实模块模型后重算 |
| 3 cm场均匀性 | 固定ROI内 $\Delta_E$ 极值 | 待有限阵列三维求解 |
| 平面桥、立体桥 | 含触点与回流的总成IL/VSWR | 假设FR4截面已有损耗风险证据，实物待求解 |
| RG142连接线 | 具体料号总成数据 | 所核型号不支持原例频率下的损耗/功率组合 |
| 最大场小于1 kV/mm | 有真实圆角、接触与异常状态的场 | 待热点模型和安全验证 |
| 500 W温升与持续工作 | 电热耦合、材料降额、负载散热 | 待验证 |
| 最终加工尺寸 | 通过鲁棒验证的冻结CAD | 尚未形成 |

## 15.4 工程师最终应交付的文件

最终三维项目应包含原生模型文件、软件版本、材料来源、完整参数表、端口定义、扫频与网格记录、所有适用工况的复数S参数、逐板功率CSV、工件吸收与热图原始数据，以及异常工况结果。PDF应明确哪些结论来自仿真、哪些来自实测。

DXF应至少分层表示板框、TOP_RF、TOP_ID、BOTTOM_GND、触点区、孔/磁铁定位、禁布区及尺寸基准；DXF本身不完整表达铜厚、叠层、镀层、材料、三维弹片和装配公差，因此还应附加工说明、层叠和三维装配文件。

本次没有生成“优化后加工DXF”，因为尚未有足够的真实几何、工作频率和三维验证来确定最终尺寸。随意画出一个能加工的线圈，并不能使它成为经证明有效的设计。

# 16. 综合结论与项目推进门槛

项目的首要问题不是优化算法不足，而是需求中存在功率预算冲突、取能与插损混用、射频回流未定义、材料损耗不足以评估以及工件边界缺失。

本次严格推导证明：500 W被动系统不能在有任何正损耗时向100块同时提供每块至少5 W；在匹配单向模型中，0.5 dB直通要求还会产生至少40.98 W末端剩余功率；相同固定比例耦合无法通过只调主机幅度纠正功率比。

本次实际二维计算进一步表明：给定层叠的2 mm能量线不能直接认定为50 Ω；以 $\tan\delta=0.02$ 的FR4假设，10 cm桥的介质损耗已超过原指标。厂家数据表核算也显示，所核RG142型号不能满足两个示例频率下的原连接线插损要求。

另一方面，按位置和N分配耦合比例在匹配等效模型内可以严格构造，完整多次反射的网络算法也已通过解析与能量守恒测试。这给出了可执行的技术起点，而不是只有抽象建议。

**进入最终三维优化的门槛：先冻结工作频率、被加热物、500 W参考面、连续/脉冲定义、空间拓扑及RF地接口，并书面修改不可同时成立的功率条款。随后以真实材料和组件模型验证耦合目标是否可实现，最后再签发三维仿真验收报告与加工图。**

\newpage

# 附录A：关键计算结果与复算方法

## A.1 文件组织

`source/rf_calculations.py`：功率守恒边界、同耦合级联、渐变耦合反算、微带基线、RG142公式及含反射网络测试。

`source/cross_section_2d.py`：二维有限体积截面求解；输出电容、准静态阻抗、材料能量参与率、网格及域尺寸敏感性。

`source/plot_results.py`：从CSV重画报告中的四张曲线。

`data/`：数值表、逐板功率、逐板耦合、网络测试及运行摘要。图形放在 `figures/`。所有数值均区分模型假设与外部材料数据，不包含虚构的HFSS/CST导出文件。

## A.2 运行

```bash
python -m pip install numpy scipy pandas matplotlib
python source/rf_calculations.py --out data
python source/cross_section_2d.py --out data
python source/plot_results.py
```

代码适用Python 3.10及以上版本。复算包另含执行时的版本清单和文件哈希。二维较细网格会使用较多内存；资源不足时可先运行粗网格，但粗网格不能替代报告中的细化检查。

## A.3 量纲和归一化检查

S参数无量纲；功率波a、b的单位为 $\sqrt{\mathrm W}$；电势二维求解单位为V；C、C0为F/m；单位长度电感为H/m；$\alpha$ 的Np/m必须通过 $20/\ln10$ 转为场幅dB/m；功率传输率应由 $10^{-IL/10}$ 而不是 $10^{-IL/20}$ 计算。

电缆公式中f使用GHz，而电磁方程、角频率和皮肤深度计算中的频率使用Hz。代码对输入范围做了基本检查，以减少单位误用。

## A.4 本次数值结果的边界

本次计算的精确性是相对于所写下的方程而言。解析链没有失配或空间互耦；合成网络用于算法测试；二维截面没有线圈、真实触点和三维工件。脚本中“有效效率1”是乐观假设，不能当作天线实测效率。由该模型求出的最大N或所需源功率，应在真实模型输入后更新。

# 附录B：外部技术依据与引用边界

[R1] HUBER+SUHNER，**Flexible RF cable RG_142_B/U**，DOC-0000177741 AN / PDO N，2019-07-22，特别是第2页的衰减和连续波功率公式。原厂编制数据表的可读取副本由经销网站托管。本报告仅把它用于指定型号的例证，不将旧版标称数据作为当前采购保证。

<https://nkt.ru/upload/tech_docs/gibkie-vch-kabeli-do-6-ggts/kabel-koaksialnyy-h-s-rg-142-b-u/huber-suhner-rg-142-bu-datasheet.pdf>

[R2] Isola，**370HR Laminate and Prepreg Data Sheet**，原厂技术资料；第2页给出频率相关Dk和Df。用于说明具体牌号参数与泛称FR4的区别，不用于给未确定板材强行赋值。

<https://www.isola-group.com/wp-content/uploads/data-sheets/370hr.pdf>

[R3] Qucs，**Single microstrip line**，官方技术文档，微带准静态阻抗、有效介电常数及介质/导体损耗。用于建立空气上覆盖的基线与损耗量纲核对；不是PP覆盖三维模型的替代品。

<https://qucs.sourceforge.net/tech/node75.html>

[R4] scikit-rf，**skrf.media.mline.MLine**，官方项目文档。说明微带模型中的介质损耗、导体厚度、粗糙度及频散的分工。本次数值脚本自行实现所述基础计算，并非调用该库输出项目全波结果。

<https://scikit-rf.readthedocs.io/en/latest/api/media/generated/skrf.media.mline.MLine.html>

[R5] COMSOL，**Introduction to the RF Module, Version 6.3**，Microwave Heating及频域电磁/热耦合说明。用于说明电磁损耗与热方程的物理耦合方式。

<https://doc.comsol.com/6.3/doc/com.comsol.help.rf/IntroductionToRFModule.pdf>

[R6] COMSOL，Henrik Sönnerlind，**Singularities in Finite Element Models: Dealing with Red Spots**，2015-06-03。用于解释理想尖角、网格相关峰值和真实圆角建模之间的区别；不是具体微波击穿阈值来源。

<https://www.comsol.com/blogs/singularities-in-finite-element-models-dealing-with-red-spots>

以上技术页面及数据表于2026年9月14日核查。项目规格本身来自用户本次提供的需求书；本文其余定理、递推式、误差传递式和数值数据由本文说明的模型推导或随附程序计算。
